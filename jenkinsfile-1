pipeline {
  agent { label 'master' }

  stages {
    stage('Hello') {
      steps {
        withCredentials(string(credentialsId: 'aws', accessKeyVariable: 'AWS_ACCESS_KEY_ID', secretKeyVariable: 'AWS_SECRET_ACCESS_KEY')) {
        ansiblePlaybook colorized: true, credentialsId: 'ssh', disableHostKeyChecking: true, installation: 'built-in', playbook: 'test-playbook.yml'
        }
      }
    }
  }
}
