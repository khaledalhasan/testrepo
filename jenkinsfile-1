pipeline {
  agent { label 'master' }

  stages {
    stage('Hello') {
      steps {
        ansiblePlaybook colorized: true, credentialsId: 'ssh', disableHostKeyChecking: true, installation: 'built-in', inventory: 'inventory.ini', playbook: 'test-playbook.yml'
      }
    }
  }
}
